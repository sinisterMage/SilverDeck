# Run greetd with the SilverDeck kiosk config (the stock /etc/greetd/config.toml
# belongs to the greetd package; shipping our own copy there would conflict).
[Service]
ExecStart=
ExecStart=/usr/bin/greetd --config /etc/greetd/silverdeck.toml
