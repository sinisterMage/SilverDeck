# Let plymouth exit cleanly before greetd takes the VT (the same ordering
# display managers ship). Harmless when plymouth is not installed: the units
# simply don't exist and the ordering is ignored.
[Unit]
After=plymouth-quit-wait.service
Wants=plymouth-quit-wait.service
